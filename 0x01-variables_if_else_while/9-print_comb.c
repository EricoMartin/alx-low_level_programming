#include <stdio.h>
#include <stdlib.h>

/**
 * main - main entry point
 *
 * description - prints all possible combinations of single-digit numbers
 *
 * Return: return zero
 */

int main(void)
{
	int num = 0;

	while (n < 10)
	{
		putchar(48 + c);
		if (c != 9)
		{
			putchar(',');
			putchar(' ');
		}
		num++;
	}
	putchar('\n')
	return (0);
}
